package yaml2jsonnet

import (
	"testing"
)

func TestProperties_Flatten(t *testing.T) {
	// node, err := ImportJsonnet("testdata/k8s.libsonnet")
	// require.NoError(t, err)

	// root, ok := node.(*ast.Object)
	// require.True(t, ok)

	// b, err := ioutil.ReadFile("testdata/deployment.yaml")
	// require.NoError(t, err)

	// var props Properties
	// err = yaml.Unmarshal(b, &props)
	// require.NoError(t, err)

	// props.Traverse(root)
}
